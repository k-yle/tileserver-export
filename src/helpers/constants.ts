export const IS_SUPERUSER = window.location.search.includes("superuser=1");

/** this many requests will be sent to the tileserver at a time */
export const CONCURRENT = 5;

export const MAX_TILES_PER_LAYER = 300;
